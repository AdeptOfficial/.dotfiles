# Menus
bindd = SUPER, SPACE, Launch apps, exec, syna-launch-walker
bindd = SUPER CTRL, E, Emoji picker, exec, syna-launch-walker -m symbols
bindd = SUPER ALT, SPACE, Adept menu, exec, syna-menu
bindd = SUPER, ESCAPE, System menu, exec, syna-menu system
bindld = , XF86PowerOff, Power menu, exec, syna-menu system
bindd = SUPER, K, Show key bindings, exec, syna-menu-keybindings
bindd = , XF86Calculator, Calculator, exec, gnome-calculator

# Aesthetics
bindd = SUPER SHIFT, SPACE, Toggle top bar, exec, syna-toggle-waybar
bindd = SUPER CTRL, SPACE, Next background in theme, exec, ~/.local/bin/rice/syna-theme-bg-next
bindd = SUPER SHIFT CTRL, SPACE, Theme menu, exec, syna-menu theme
bindd = SUPER, BACKSPACE, Toggle window transparency, exec, hyprctl dispatch setprop "address:$(hyprctl activewindow -j | jq -r '.address')" opaque toggle
bindd = SUPER SHIFT, BACKSPACE, Toggle workspace gaps, exec, syna-hyprland-workspace-toggle-gaps

# Notifications
bindd = SUPER, COMMA, Dismiss last notification, exec, makoctl dismiss
bindd = SUPER SHIFT, COMMA, Dismiss all notifications, exec, makoctl dismiss --all
bindd = SUPER CTRL, COMMA, Toggle silencing notifications, exec, makoctl mode -t do-not-disturb && makoctl mode | grep -q 'do-not-disturb' && notify-send "Silenced notifications" || notify-send "Enabled notifications"
bindd = SUPER ALT, COMMA, Invoke last notification, exec, makoctl invoke
bindd = SUPER SHIFT ALT, COMMA, Restore last notification, exec, makoctl restore

# Toggle idling
bindd = SUPER CTRL, I, Toggle locking on idle, exec, syna-toggle-idle

# Toggle nightlight
bindd = SUPER CTRL, N, Toggle nightlight, exec, syna-toggle-nightlight

# Control Apple Display brightness
bindd = CTRL, F1, Apple Display brightness down, exec, syna-cmd-apple-display-brightness -5000
bindd = CTRL, F2, Apple Display brightness up, exec, syna-cmd-apple-display-brightness +5000
bindd = SHIFT CTRL, F2, Apple Display full brightness, exec, syna-cmd-apple-display-brightness +60000

# File sharing
bindd = SUPER CTRL, S, Share, exec, syna-menu share

# Waybar-less information
bindd = SUPER CTRL ALT, T, Show time, exec, notify-send "    $(date +"%A %H:%M  —  %d %B W%V %Y")"
bindd = SUPER CTRL ALT, B, Show battery remaining, exec, notify-send "󰁹    Battery is at $(syna-battery-remaining)%"

# Control panels
bindd = SUPER CTRL, A, Audio controls, exec, syna-launch-audio
bindd = SUPER CTRL, B, Bluetooth controls, exec, syna-launch-bluetooth
bindd = SUPER CTRL, W, Wifi controls, exec, syna-launch-wifi
bindd = SUPER CTRL, T, Activity, exec, syna-launch-tui btop

# Dictation
bindd  = SUPER CTRL, X, Start dictation, exec, voxtype record start
binddr = SUPER CTRL, X, Stop dictation, exec, voxtype record stop

# Lock system
bindd = SUPER CTRL, L, Lock system, exec, syna-lock-screen

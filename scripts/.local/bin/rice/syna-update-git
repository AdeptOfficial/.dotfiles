#!/bin/bash
# Update dotfiles from git

set -e

if [[ ! -d "$HOME/.dotfiles/.git" ]]; then
  echo "No git repo at ~/.dotfiles"
  exit 1
fi

echo -e "\e[32mUpdating dotfiles\e[0m"

cd ~/.dotfiles
git pull --autostash

echo "Done. You may need to re-stow packages if structure changed."

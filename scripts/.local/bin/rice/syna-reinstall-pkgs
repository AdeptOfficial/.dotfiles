#!/bin/bash
# Reinstall packages
# For dotfiles setups, run the install script again or use pacman/yay directly.

echo "Package management for dotfiles setup:"
echo ""
echo "  Update all packages:  yay -Syu"
echo "  Reinstall from list:  cd ~/.dotfiles && ./install.sh"
echo ""
echo "Package lists are defined in ~/.dotfiles/install.sh"

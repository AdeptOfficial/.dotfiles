#!/bin/bash

# Legacy script - not used on standalone CachyOS setup.
# Original purpose: create migration scripts for Omarchy updates.

echo "This script is not used on standalone CachyOS setup."
exit 1

# cd ~/.local/share/omarchy
# migration_file="$HOME/.local/share/omarchy/migrations/$(git log -1 --format=%cd --date=unix).sh"
touch $migration_file

if [[ "$1" != "--no-edit" ]]; then
  nvim $migration_file
fi

echo $migration_file

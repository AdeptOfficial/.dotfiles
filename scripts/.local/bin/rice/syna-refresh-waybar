#!/bin/bash

# Overwrite the user configs for the Waybar menu bar with the rice defaults and restart the service.

syna-refresh-config waybar/config.jsonc
syna-refresh-config waybar/style.css
syna-restart-waybar

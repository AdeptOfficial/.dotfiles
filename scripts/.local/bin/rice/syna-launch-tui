#!/bin/bash

# Launch the TUI command passed in as an argument in the default terminal with an io.adept.rice.COMMAND app id for styling.

exec setsid uwsm-app -- xdg-terminal-exec --app-id=io.adept.rice.$(basename $1) -e "$1" "${@:2}"
